apiVersion: apps/v1
kind: Deployment
metadata:
  name: sc-netcore3-deployment
spec:
  selector:
    matchLabels:
      app: sc-netcore3-pod
  template:
    metadata:
      labels:
        app: sc-netcore3-pod
    spec:
      containers:
      - name: sc-netcore3-container
        image: localhost:32000/netv13:latest
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 80